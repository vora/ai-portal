import { Layout, Input, Menu, Breadcrumb, Row, Col, Card } from 'antd';

const { Header, Content, Footer } = Layout;
const { Search } = Input;
const { Meta } = Card;

export {
  Layout,
  Header,
  Content,
  Footer,
  Menu,
  Breadcrumb,
  Search,
  Row,
  Col,
  Card,
  Meta,
};
